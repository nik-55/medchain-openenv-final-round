OpenEnv standardizes RL environments for LLM training — define once with a Gymnasium-style `reset()` / `step()` API, serve over WebSockets in Docker, deploy to HF Spaces, and any RL framework plugs straight in. Reproducible, shareable, and scalable. Hackathon submission was built on top of this.

